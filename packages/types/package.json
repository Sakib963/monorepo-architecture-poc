{
  "name": "@poc/types",
  "version": "1.0.0",
  "description": "Shared TypeScript interfaces, enums and DTOs used across all apps and services",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "~5.5.0"
  }
}

# Types Package

Shared TypeScript types used across all applications and services.

## Types Included

- `User` - User model
- `Role` - User role enum (USER, ADMIN)
- `LoginRequest` - Login request payload
- `AuthResponse` - Authentication response

## Usage

```typescript
import { User, Role, AuthResponse } from '@monorepo/types';
```

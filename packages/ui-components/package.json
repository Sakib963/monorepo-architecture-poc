{
  "name": "@poc/ui-components",
  "version": "1.0.0",
  "description": "Shared Angular component library — same Button, Card, Badge and StatusIndicator used by both the showcase hub and the team-angular app",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "ng-packagr -p ng-package.json",
    "dev": "ng-packagr -p ng-package.json --watch",
    "lint": "eslint ."
  },
  "peerDependencies": {
    "@angular/common": ">=18.0.0",
    "@angular/core": ">=18.0.0"
  },
  "devDependencies": {
    "@angular/compiler": "~18.2.0",
    "@angular/compiler-cli": "~18.2.0",
    "@angular/core": "~18.2.0",
    "@angular/common": "~18.2.0",
    "ng-packagr": "~18.2.0",
    "typescript": "~5.5.0"
  }
}

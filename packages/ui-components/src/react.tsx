/**
 * @poc/ui-components/react
 *
 * React versions of UI components from @poc/ui-components.
 *
 * Usage:
 *   import { Button } from '@poc/ui-components/react'
 *
 * Currently, Angular components are in the main package exports.
 * React components are in the ./react subdirectory to avoid conflicts.
 */

export { Button } from './button/button.react';
export type { ButtonVariant, ButtonSize, ButtonProps } from './button/button.react';

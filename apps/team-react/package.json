{
  "name": "@poc/team-react",
  "version": "1.0.0",
  "description": "Team A product app — React 18 + Vite, uses the same @poc/types, @poc/validators and @poc/api-client as the Angular team app",
  "private": true,
  "scripts": {
    "dev": "vite --port 4201",
    "build": "tsc -p tsconfig.app.json && vite build",
    "preview": "vite preview",
    "lint": "eslint ."
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "~5.5.0",
    "vite": "^5.3.4"
  }
}

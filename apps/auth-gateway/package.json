{
  "name": "@monorepo/auth-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Authentication gateway - login interface",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@monorepo/auth-client": "*",
    "@monorepo/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^4.4.0"
  }
}
